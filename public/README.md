# Static assets

Put **resume.pdf** here so the “View Resume” links (title screen, Let’s Connect, completion overlay) open your PDF. The app uses `content.meta.resumePageUrl` (default `/resume.pdf`). See **GO-LIVE-STEPS.md** → Step 2.2.
